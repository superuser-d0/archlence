name: Release

on:
  push:
    tags: [ "v*" ]

  workflow_dispatch:
    inputs:
      version:
        description: "Release version (for example, 1.0.0)"
        required: true
        type: string

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false   # Never interrupt a release in progress.

permissions:
  contents: write   # Required to create releases and upload assets.

jobs:
  version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.derive.outputs.version }}
      prerelease: ${{ steps.derive.outputs.prerelease }}
    steps:
      - name: Check out source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4

      - name: Derive version
        id: derive
        shell: bash

        run: |
          if [ "${{ github.event_name }}" = "push" ]; then
            version="${GITHUB_REF#refs/tags/}"
            version="${version#v}"
          else
            version="${{ inputs.version }}"
          fi
          if [ -z "$version" ]; then
            echo "::error::Unable to derive the release version."
            exit 1
          fi
          expected="$(python -c 'from utils.version import APP_VERSION; print(APP_VERSION)')"
          if [ "$version" != "$expected" ]; then
            echo "::error::Tag/input version ($version) does not match application version ($expected)."
            exit 1
          fi

          case "$version" in
            0.*|*-*) prerelease=true ;;
            *)       prerelease=false ;;
          esac

          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "prerelease=$prerelease" >> "$GITHUB_OUTPUT"
          echo "Version: $version (prerelease: $prerelease)"

  build-windows:
    needs: version
    uses: ./.github/workflows/build-windows.yml
    with:
      version: ${{ needs.version.outputs.version }}

  build-linux:
    needs: version
    uses: ./.github/workflows/build-linux.yml

  publish:
    needs: [version, build-windows, build-linux]
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4

      - name: Download Windows installer
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        with:
          name: Archlence-Setup
          path: assets-in/windows

      - name: Download Linux AppImage
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4
        with:
          name: Archlence-AppImage
          path: assets-in/linux

      - name: Prepare versioned assets
        shell: bash

        run: |
          set -euo pipefail
          v="${{ needs.version.outputs.version }}"
          mkdir -p release-assets
          cp "assets-in/windows/ArchlenceSetup.exe" \
             "release-assets/ArchlenceSetup-${v}.exe"
          cp "assets-in/linux/Archlence-x86_64.AppImage" \
             "release-assets/Archlence-${v}-x86_64.AppImage"
          cp THIRD_PARTY_NOTICES.md release-assets/
          (
            cd release-assets
            sha256sum "ArchlenceSetup-${v}.exe" \
              "Archlence-${v}-x86_64.AppImage" \
              THIRD_PARTY_NOTICES.md > SHA256SUMS.txt
          )
          ls -la release-assets

      - name: Generate release notes from CHANGELOG
        shell: bash
        run: |
          python scripts/release_notes_from_changelog.py \
            "${{ needs.version.outputs.version }}" \
            --output release-notes.md
          cat release-notes.md

      - name: Generate CycloneDX SBOM
        shell: bash
        run: |
          python -m pip install cyclonedx-bom
          cyclonedx-py requirements requirements-runtime.txt \
            --output-format JSON \
            --output-file "release-assets/Archlence-${{ needs.version.outputs.version }}-sbom.cdx.json"
          (
            cd release-assets
            sha256sum "Archlence-${{ needs.version.outputs.version }}-sbom.cdx.json" \
              >> SHA256SUMS.txt
          )

      - name: Create draft release and upload assets
        shell: bash

        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          v="${{ needs.version.outputs.version }}"
          if [ "${{ github.event_name }}" = "push" ]; then
            tag="${{ github.ref_name }}"
          else
            tag="v${v}"
          fi
          if [ "${{ needs.version.outputs.prerelease }}" = "true" ]; then
            prerelease_flag="--prerelease"
          else
            prerelease_flag=""
          fi
          gh release create "$tag" release-assets/* \
            --repo "${{ github.repository }}" \
            --title "Archlence v${v}" \
            --notes-file release-notes.md \
            --target "${{ github.sha }}" \
            $prerelease_flag \
            --draft

      - name: Download assets again and verify SHA-256
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          tag="v${{ needs.version.outputs.version }}"
          mkdir downloaded
          gh release download "$tag" \
            --repo "${{ github.repository }}" \
            --dir downloaded
          cp downloaded/SHA256SUMS.txt downloaded/expected.txt
          (
            cd downloaded
            sha256sum -c expected.txt
          )

      - name: Publish tag-triggered release with correct stability state
        if: github.event_name == 'push'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

        run: |
          set -euo pipefail
          tag="${{ github.ref_name }}"
          if [ "${{ needs.version.outputs.prerelease }}" = "true" ]; then

            gh release edit "$tag" \
              --repo "${{ github.repository }}" \
              --draft=false \
              --prerelease=true
            echo "Published as a prerelease (not Latest)."
          else
            gh release edit "$tag" \
              --repo "${{ github.repository }}" \
              --draft=false \
              --prerelease=false \
              --latest
            echo "Published as the stable Latest release."
          fi
          echo "Published: $(gh release view "$tag" --repo "${{ github.repository }}" --json url -q .url)"
