# Yalnızca geliştirme/lint araçları — runtime bağımlılıklarını İÇERMEZ
# (CI'daki lint job'ı bu yüzden hızlı kalıyor: kivy/pandas/numpy'yi kurmadan
# yalnızca bunları kurar). Tam geliştirme ortamı için requirements.txt
# şemsiyesini kullan (ikisini birden kurar), ya da elle:
#   pip install -r requirements-runtime.txt -r requirements-dev.txt
#
# Çalışan uygulama bunlara hiç ihtiyaç duymaz (bkz. archlence.spec'in
# excludes listesi, aynı dört paketi bu gerekçeyle paketlenmiş .exe'den de
# çıkarıyor).
# Property-based testler (scripts/audit/test_phase2_financial_properties.py)
# yalnızca DENETİM tarafında kullanılıyor. Runtime bağımlılığı DEĞİL —
# archlence.spec'in excludes listesindeki gerekçenin aynısı geçerli.
# Bağımlılık güvenlik taraması. Phase 2'den beri "Not started" duruyordu;
# ilk koşumda 3 pakette 18 benzersiz bulgu çıktı (pillow'unkiler
# `services/brand_icon_service.py` üzerinden ağdan gelen görüntülerle
# ERİŞİLEBİLİRDİ, bkz. CHANGELOG). Artık `reliability-gates` içinde koşuyor,
# yani sonraki bulgu sessizce birikmiyor.
pip-audit==2.10.1
hypothesis==6.165.2
flake8==7.3.0
ast-serialize==0.6.0
librt==0.13.0
pycodestyle==2.14.0
pyflakes==3.4.0
mccabe==0.7.0
mypy==2.3.0
mypy_extensions==1.1.0
pathspec==1.1.1
